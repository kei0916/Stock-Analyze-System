# Phase 4: CLI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the complete CLI interface with argparse subcommands, async handler dispatch, shared DI assembly, and output formatting — fixing 4 known bugs and 4 newly discovered bugs from the reference project.

**Architecture:** The CLI entry point (`__main__.py`) calls `asyncio.run(main())`. `app.py` builds the argparse parser tree and dispatches to per-module async handlers. A `ServiceContainer` dataclass + `setup_services()` factory function provides DI assembly shared between CLI and future Web layers. Each CLI module receives `(args, services)` for consistent handler signatures. `screen` and `rag` commands are registered as stubs (Phase 5/6 dependencies).

**Tech Stack:** argparse (stdlib), asyncio, tabulate, SQLAlchemy AsyncSession, existing Phase 2 clients + Phase 3 services

---

## Reference Project Bug Analysis

### Verified Known Bugs (Phase 4)

| # | Bug | Fix |
|---|------|-----|
| 3 | `jobs --type` defined but never used; always calls `run_daily_update()` | Remove `--type`. Subcommands: `sync <company_id>` and `daily --market` |
| 15 | `serve` port uses `args.port or config.web.port` — port 0 is falsy | Use `args.port if args.port is not None else config.web.port` |
| 16 | `valuation` usage string omits `deviation` subcommand | Remove manual usage; let argparse auto-generate help |
| 17 | `watchlist` handlers take only `(args)` while all others take `(args, config)` | All handlers take `(args, services)` |

### Newly Discovered Bugs

| # | Bug | Fix |
|---|------|-----|
| 新1 | `serve` no port range validation — accepts -1, 99999 | Validate 1 ≤ port ≤ 65535 |
| 新2 | `screen` has duplicate `--limit` args with different defaults/semantics | Separate: `--limit` for results, `--enrich-limit` for refresh |
| 新3 | `screen` uses `getattr(args, "json", False)` defensively | Use direct `args.json` (globally defined) |
| 新4 | Inconsistent not-found error handling — some `return`, some `sys.exit(1)` | Standardize: always `sys.exit(1)` via `require_company()` helper |

---

## File Structure

| File | Responsibility | Status |
|------|---------------|--------|
| `src/stock_analyze_system/cli/__init__.py` | Package init | Create |
| `src/stock_analyze_system/cli/formatters.py` | `format_table`, `format_json`, `fmt_number`, `fmt_large` | Create |
| `src/stock_analyze_system/cli/helpers.py` | `ServiceContainer`, `setup_services`, `require_company`, `require_latest_filing` | Create |
| `src/stock_analyze_system/cli/app.py` | `build_parser`, `main` — entry point + parser tree | Create |
| `src/stock_analyze_system/__main__.py` | `asyncio.run(main())` | Create |
| `src/stock_analyze_system/cli/company.py` | `company register/search/show` | Create |
| `src/stock_analyze_system/cli/financial.py` | `financial show/metrics` | Create |
| `src/stock_analyze_system/cli/valuation.py` | `valuation show/compare/range/deviation` (Bug #16) | Create |
| `src/stock_analyze_system/cli/filings.py` | `filings list/download` | Create |
| `src/stock_analyze_system/cli/jobs.py` | `jobs sync/daily` (Bug #3) | Create |
| `src/stock_analyze_system/cli/watchlist.py` | `watchlist create/list/show/add/remove` (Bug #17) | Create |
| `src/stock_analyze_system/cli/target.py` | `target list/add/remove` | Create |
| `src/stock_analyze_system/cli/serve.py` | `serve` (Bug #15, 新1) | Create |
| `src/stock_analyze_system/cli/screen.py` | `screen run/add-targets` (stub — Phase 5) | Create |
| `src/stock_analyze_system/cli/rag.py` | `rag analyze/ask/index/status/health/show` (stub — Phase 6) | Create |
| `tests/unit/cli/__init__.py` | Test package init | Create |
| `tests/unit/cli/test_formatters.py` | Formatter unit tests | Create |
| `tests/unit/cli/test_helpers.py` | Helper unit tests | Create |
| `tests/unit/cli/test_company_cli.py` | Company CLI handler tests | Create |
| `tests/unit/cli/test_financial_cli.py` | Financial CLI handler tests | Create |
| `tests/unit/cli/test_valuation_cli.py` | Valuation CLI handler tests | Create |
| `tests/unit/cli/test_filings_cli.py` | Filings CLI handler tests | Create |
| `tests/unit/cli/test_jobs_cli.py` | Jobs CLI handler tests (Bug #3) | Create |
| `tests/unit/cli/test_watchlist_cli.py` | Watchlist CLI handler tests (Bug #17) | Create |
| `tests/unit/cli/test_target_cli.py` | Target CLI handler tests | Create |
| `tests/unit/cli/test_serve_cli.py` | Serve CLI handler tests (Bug #15, 新1) | Create |

---

## Common Patterns

### Handler Signature

All CLI handlers follow this pattern:

```python
async def handle(args: argparse.Namespace, services: ServiceContainer) -> None:
```

### Async Dispatch in app.py

```python
await args.handler(args, services)
```

### Output Pattern

```python
if args.json:
    print(format_json(data))
else:
    print(format_table(rows))
```

### Error Pattern

```python
company = await require_company(services.company_service, args.company_id)
# require_company calls sys.exit(1) if not found
```

---

## Task 1: CLI Foundation — formatters.py + helpers.py

**Files:**
- Create: `src/stock_analyze_system/cli/__init__.py`
- Create: `src/stock_analyze_system/cli/formatters.py`
- Create: `src/stock_analyze_system/cli/helpers.py`
- Create: `tests/unit/cli/__init__.py`
- Create: `tests/unit/cli/test_formatters.py`
- Create: `tests/unit/cli/test_helpers.py`

### Step 1: Write formatters tests

```python
# tests/unit/cli/__init__.py
```

```python
# tests/unit/cli/test_formatters.py
"""CLI formatters のテスト"""
import json

from stock_analyze_system.cli.formatters import (
    fmt_large,
    fmt_number,
    format_json,
    format_table,
)


class TestFmtNumber:
    def test_normal(self):
        assert fmt_number(3.14159, precision=2) == "3.14"

    def test_none(self):
        assert fmt_number(None) == "N/A"

    def test_zero(self):
        assert fmt_number(0.0) == "0.00"


class TestFmtLarge:
    def test_billion(self):
        result = fmt_large(2_500_000_000.0)
        assert "B" in result

    def test_million(self):
        result = fmt_large(45_000_000.0)
        assert "M" in result

    def test_thousand(self):
        result = fmt_large(8_500.0)
        assert "K" in result

    def test_small(self):
        result = fmt_large(500.0)
        assert result == "500.00"

    def test_none(self):
        assert fmt_large(None) == "N/A"


class TestFormatTable:
    def test_basic(self):
        data = [{"Name": "Apple", "Price": 185.0}]
        result = format_table(data)
        assert "Apple" in result
        assert "185" in result

    def test_empty(self):
        result = format_table([])
        assert "No data" in result

    def test_custom_headers(self):
        data = [{"a": 1, "b": 2, "c": 3}]
        result = format_table(data, headers=["a", "b"])
        assert "1" in result


class TestFormatJson:
    def test_dict(self):
        result = format_json({"key": "value"})
        parsed = json.loads(result)
        assert parsed["key"] == "value"

    def test_list(self):
        result = format_json([1, 2, 3])
        parsed = json.loads(result)
        assert parsed == [1, 2, 3]

    def test_non_serializable(self):
        """date等の非標準型もstr変換される"""
        from datetime import date
        result = format_json({"d": date(2024, 1, 1)})
        parsed = json.loads(result)
        assert "2024-01-01" in parsed["d"]
```

- [ ] **Step 2: Run formatters tests — verify they fail**

Run: `cd <repo-root> && python3 -m pytest tests/unit/cli/test_formatters.py -v --no-header 2>&1 | tail -20`
Expected: FAIL (ModuleNotFoundError)

- [ ] **Step 3: Implement formatters.py**

```python
# src/stock_analyze_system/cli/__init__.py
```

```python
# src/stock_analyze_system/cli/formatters.py
"""CLI出力フォーマッタ"""
from __future__ import annotations

import json
from typing import Any

from tabulate import tabulate


def fmt_number(value: float | None, precision: int = 2) -> str:
    """数値をフォーマット。Noneは'N/A'"""
    if value is None:
        return "N/A"
    return f"{value:.{precision}f}"


def fmt_large(value: float | None, precision: int = 2) -> str:
    """大きな数値をB/M/K付きでフォーマット"""
    if value is None:
        return "N/A"
    abs_val = abs(value)
    if abs_val >= 1e9:
        return f"{value / 1e9:.{precision}f}B"
    if abs_val >= 1e6:
        return f"{value / 1e6:.{precision}f}M"
    if abs_val >= 1e3:
        return f"{value / 1e3:.{precision}f}K"
    return f"{value:.{precision}f}"


def format_table(data: list[dict], headers: list[str] | None = None) -> str:
    """辞書リストをテーブル文字列に変換"""
    if not data:
        return "No data available."
    if headers is None:
        headers = list(data[0].keys())
    rows = [[str(row.get(h, "")) for h in headers] for row in data]
    return tabulate(rows, headers=headers, tablefmt="simple")


def format_json(data: Any) -> str:
    """JSONフォーマット出力"""
    return json.dumps(data, indent=2, ensure_ascii=False, default=str)
```

- [ ] **Step 4: Run formatters tests — verify they pass**

Run: `cd <repo-root> && python3 -m pytest tests/unit/cli/test_formatters.py -v --no-header 2>&1 | tail -20`
Expected: ALL PASSED

- [ ] **Step 5: Write helpers tests**

```python
# tests/unit/cli/test_helpers.py
"""CLI helpers のテスト"""
from dataclasses import dataclass
from unittest.mock import AsyncMock, MagicMock

import pytest

from stock_analyze_system.cli.helpers import (
    ServiceContainer,
    require_company,
    require_latest_filing,
    setup_services,
)


class TestRequireCompany:
    async def test_found(self):
        svc = AsyncMock()
        company = MagicMock()
        company.id = "US_AAPL"
        svc.get_company.return_value = company
        result = await require_company(svc, "US_AAPL")
        assert result.id == "US_AAPL"

    async def test_not_found_exits(self):
        svc = AsyncMock()
        svc.get_company.return_value = None
        with pytest.raises(SystemExit) as exc_info:
            await require_company(svc, "US_AAPL")
        assert exc_info.value.code == 1


class TestRequireLatestFiling:
    async def test_found(self):
        svc = AsyncMock()
        filing = MagicMock()
        filing.id = 1
        svc.get_latest_filing.return_value = filing
        result = await require_latest_filing(svc, "US_AAPL", "10-K")
        assert result.id == 1

    async def test_not_found_exits(self):
        svc = AsyncMock()
        svc.get_latest_filing.return_value = None
        with pytest.raises(SystemExit) as exc_info:
            await require_latest_filing(svc, "US_AAPL", "10-K")
        assert exc_info.value.code == 1


class TestServiceContainer:
    def test_all_fields_accessible(self):
        container = ServiceContainer(
            company_service=MagicMock(),
            financial_service=MagicMock(),
            valuation_service=MagicMock(),
            filing_service=MagicMock(),
            watchlist_service=MagicMock(),
            target_service=MagicMock(),
            job_service=MagicMock(),
            financial_sync=MagicMock(),
            filing_sync=MagicMock(),
            screening_service=None,
            rag_service=None,
        )
        assert container.company_service is not None
        assert container.screening_service is None


class TestSetupServices:
    async def test_returns_container(self):
        """setup_services が ServiceContainer を返すこと"""
        from stock_analyze_system.config import AppConfig
        from sqlalchemy.ext.asyncio import create_async_engine, AsyncSession
        from sqlalchemy.ext.asyncio import async_sessionmaker

        engine = create_async_engine("sqlite+aiosqlite:///:memory:")
        factory = async_sessionmaker(engine, expire_on_commit=False)
        async with factory() as session:
            config = AppConfig()
            container = await setup_services(session, config)
            assert isinstance(container, ServiceContainer)
            assert container.company_service is not None
            assert container.job_service is not None
        await engine.dispose()
```

- [ ] **Step 6: Run helpers tests — verify they fail**

Run: `cd <repo-root> && python3 -m pytest tests/unit/cli/test_helpers.py -v --no-header 2>&1 | tail -20`
Expected: FAIL (ModuleNotFoundError)

- [ ] **Step 7: Implement helpers.py**

```python
# src/stock_analyze_system/cli/helpers.py
"""CLI共通ヘルパー: DI組み立て + リソース検証"""
from __future__ import annotations

import sys
from dataclasses import dataclass
from typing import Any

from sqlalchemy.ext.asyncio import AsyncSession

from stock_analyze_system.config import AppConfig


@dataclass
class ServiceContainer:
    """CLI/Web共通のサービスコンテナ"""
    company_service: Any
    financial_service: Any
    valuation_service: Any
    filing_service: Any
    watchlist_service: Any
    target_service: Any
    job_service: Any
    financial_sync: Any
    filing_sync: Any
    screening_service: Any = None
    rag_service: Any = None


async def setup_services(
    session: AsyncSession, config: AppConfig,
) -> ServiceContainer:
    """リポジトリ・サービスの依存グラフを組み立て"""
    from stock_analyze_system.ingestion.sec_edgar import SecEdgarClient
    from stock_analyze_system.ingestion.edinet import EdinetClient
    from stock_analyze_system.ingestion.yahoo_finance import YahooFinanceClient
    from stock_analyze_system.ingestion.fmp import FmpClient
    from stock_analyze_system.repositories.company import CompanyRepository
    from stock_analyze_system.repositories.financial import FinancialRepository
    from stock_analyze_system.repositories.valuation import ValuationRepository
    from stock_analyze_system.repositories.filing import FilingRepository
    from stock_analyze_system.repositories.watchlist import WatchlistRepository
    from stock_analyze_system.repositories.target import TargetRepository
    from stock_analyze_system.services.company import CompanyService
    from stock_analyze_system.services.financial import FinancialService
    from stock_analyze_system.services.valuation import ValuationService
    from stock_analyze_system.services.filing import FilingService
    from stock_analyze_system.services.watchlist import WatchlistService
    from stock_analyze_system.services.analysis_target import AnalysisTargetService
    from stock_analyze_system.services.financial_sync import FinancialSyncService
    from stock_analyze_system.services.filing_sync import FilingSyncService
    from stock_analyze_system.services.job import JobService

    # 1. API クライアント生成
    sec_client = SecEdgarClient(email=config.sec_edgar.email)
    edinet_client = EdinetClient(
        api_key=config.edinet.api_key,
        base_url=config.edinet.base_url,
    )
    yahoo_client = YahooFinanceClient(rate=config.yahoo_finance.rate_limit_rps)
    fmp_client = FmpClient(
        api_key=config.fmp.api_key,
        base_url=config.fmp.base_url,
    )

    # 2. リポジトリ生成
    company_repo = CompanyRepository(session)
    financial_repo = FinancialRepository(session)
    valuation_repo = ValuationRepository(session)
    filing_repo = FilingRepository(session)
    watchlist_repo = WatchlistRepository(session)
    target_repo = TargetRepository(session)

    # 3. ドメインサービス生成
    company_svc = CompanyService(company_repo)
    financial_svc = FinancialService(financial_repo)
    valuation_svc = ValuationService(valuation_repo)
    filing_svc = FilingService(filing_repo)
    watchlist_svc = WatchlistService(watchlist_repo)
    target_svc = AnalysisTargetService(target_repo)

    # 4. Sync/Orchestrationサービス生成
    financial_sync = FinancialSyncService(
        financial_repo, sec_client, edinet_client, yahoo_client, fmp_client,
    )
    filing_sync = FilingSyncService(filing_repo, sec_client, edinet_client)
    job_svc = JobService(
        company_svc, financial_sync, filing_sync,
        valuation_svc, financial_svc, yahoo_client, fmp_client,
    )

    return ServiceContainer(
        company_service=company_svc,
        financial_service=financial_svc,
        valuation_service=valuation_svc,
        filing_service=filing_svc,
        watchlist_service=watchlist_svc,
        target_service=target_svc,
        job_service=job_svc,
        financial_sync=financial_sync,
        filing_sync=filing_sync,
    )


async def require_company(company_service: Any, company_id: str) -> Any:
    """企業を検索し、未検出なら sys.exit(1)"""
    company = await company_service.get_company(company_id)
    if company is None:
        print(f"Company '{company_id}' not found.", file=sys.stderr)
        sys.exit(1)
    return company


async def require_latest_filing(
    filing_service: Any, company_id: str, filing_type: str,
) -> Any:
    """最新ファイリングを検索し、未検出なら sys.exit(1)"""
    filing = await filing_service.get_latest_filing(company_id, filing_type)
    if filing is None:
        print(
            f"No '{filing_type}' filings found for '{company_id}'.",
            file=sys.stderr,
        )
        sys.exit(1)
    return filing
```

- [ ] **Step 8: Run helpers tests — verify they pass**

Run: `cd <repo-root> && python3 -m pytest tests/unit/cli/test_helpers.py -v --no-header 2>&1 | tail -20`
Expected: ALL PASSED

- [ ] **Step 9: Commit**

```bash
cd <repo-root>
git add src/stock_analyze_system/cli/__init__.py src/stock_analyze_system/cli/formatters.py src/stock_analyze_system/cli/helpers.py tests/unit/cli/__init__.py tests/unit/cli/test_formatters.py tests/unit/cli/test_helpers.py
git commit -m "feat: add CLI foundation - formatters, helpers, ServiceContainer, setup_services"
```

---

## Task 2: app.py + __main__.py — Entry Point

**Files:**
- Create: `src/stock_analyze_system/__main__.py`
- Create: `src/stock_analyze_system/cli/app.py`
- Test: `tests/unit/cli/test_app.py`

- [ ] **Step 1: Write app tests**

```python
# tests/unit/cli/test_app.py
"""app.py のテスト"""
from stock_analyze_system.cli.app import build_parser


class TestBuildParser:
    def test_creates_parser(self):
        parser = build_parser()
        assert parser.prog == "stock-analyze"

    def test_global_json_flag(self):
        parser = build_parser()
        args = parser.parse_args(["--json", "company", "search", "Apple"])
        assert args.json is True

    def test_no_command_defaults(self):
        parser = build_parser()
        args = parser.parse_args([])
        assert args.command is None

    def test_company_subcommand(self):
        parser = build_parser()
        args = parser.parse_args(["company", "search", "Apple"])
        assert args.command == "company"
        assert args.action == "search"

    def test_config_flag(self):
        parser = build_parser()
        args = parser.parse_args(["--config", "/tmp/test.yaml", "company", "search", "x"])
        assert args.config == "/tmp/test.yaml"
```

- [ ] **Step 2: Run tests — verify they fail**

Run: `cd <repo-root> && python3 -m pytest tests/unit/cli/test_app.py -v --no-header 2>&1 | tail -20`
Expected: FAIL

- [ ] **Step 3: Implement app.py and __main__.py**

```python
# src/stock_analyze_system/__main__.py
"""パッケージエントリポイント: python -m stock_analyze_system"""
import asyncio

from stock_analyze_system.cli.app import main

asyncio.run(main())
```

```python
# src/stock_analyze_system/cli/app.py
"""CLIルートパーサーとディスパッチャ"""
from __future__ import annotations

import argparse
import logging
import sys

from stock_analyze_system.cli import (
    company,
    filings,
    financial,
    jobs,
    rag,
    screen,
    serve,
    target,
    valuation,
    watchlist,
)
from stock_analyze_system.config import load_config
from stock_analyze_system.models.base import create_db_engine, get_session


def build_parser() -> argparse.ArgumentParser:
    """argparse パーサーツリーを構築"""
    parser = argparse.ArgumentParser(
        prog="stock-analyze",
        description="Stock Analyze System — US/JP 株式財務分析CLI",
    )
    parser.add_argument(
        "--json", action="store_true", default=False,
        help="JSON形式で出力",
    )
    parser.add_argument(
        "--config", type=str, default="config/settings.yaml",
        help="設定ファイルパス",
    )
    parser.add_argument(
        "--db-path", type=str, default=None,
        help="データベースパス上書き",
    )

    subparsers = parser.add_subparsers(dest="command")

    company.register_parser(subparsers)
    financial.register_parser(subparsers)
    valuation.register_parser(subparsers)
    filings.register_parser(subparsers)
    jobs.register_parser(subparsers)
    watchlist.register_parser(subparsers)
    target.register_parser(subparsers)
    screen.register_parser(subparsers)
    rag.register_parser(subparsers)
    serve.register_parser(subparsers)

    return parser


async def main() -> None:
    """CLIメインエントリポイント"""
    parser = build_parser()
    args = parser.parse_args()

    if args.command is None:
        parser.print_help()
        sys.exit(1)

    config = load_config(args.config)
    if args.db_path:
        config.database.path = args.db_path

    logging.basicConfig(
        level=getattr(logging, config.logging.level, logging.INFO),
    )

    # serve は独自のライフサイクルを持つ
    if args.command == "serve":
        await args.handler(args, config)
        return

    from stock_analyze_system.cli.helpers import setup_services

    engine = await create_db_engine(config.database.path)
    try:
        async with get_session(engine) as session:
            services = await setup_services(session, config)
            await args.handler(args, services)
    finally:
        await engine.dispose()
```

**注意**: `build_parser` は各CLIモジュールの `register_parser` を呼ぶため、Task 3〜10のスタブが必要。Task 2のテスト実行はTask 10完了後に一括で行う。各モジュールは Task 3〜10 で実装するが、`register_parser` のスタブだけは本タスクで全て作成する。

以下のスタブファイルも同時に作成する:

```python
# src/stock_analyze_system/cli/company.py
"""企業管理サブコマンド"""
from __future__ import annotations
import argparse


def register_parser(subparsers: argparse._SubParsersAction) -> None:
    parser = subparsers.add_parser("company", help="企業管理")
    parser.add_subparsers(dest="action")
    parser.set_defaults(handler=handle)


async def handle(args: argparse.Namespace, services) -> None:
    pass
```

上記と同じスタブパターンで以下も作成:
- `financial.py` (command="financial")
- `valuation.py` (command="valuation")
- `filings.py` (command="filings")
- `jobs.py` (command="jobs")
- `watchlist.py` (command="watchlist")
- `target.py` (command="target")
- `screen.py` (command="screen")
- `rag.py` (command="rag")
- `serve.py` (command="serve", handler takes `(args, config)` not `(args, services)`)

- [ ] **Step 4: Run tests — verify they pass**

Run: `cd <repo-root> && python3 -m pytest tests/unit/cli/test_app.py -v --no-header 2>&1 | tail -20`
Expected: ALL PASSED

- [ ] **Step 5: Commit**

```bash
cd <repo-root>
git add src/stock_analyze_system/__main__.py src/stock_analyze_system/cli/app.py src/stock_analyze_system/cli/company.py src/stock_analyze_system/cli/financial.py src/stock_analyze_system/cli/valuation.py src/stock_analyze_system/cli/filings.py src/stock_analyze_system/cli/jobs.py src/stock_analyze_system/cli/watchlist.py src/stock_analyze_system/cli/target.py src/stock_analyze_system/cli/screen.py src/stock_analyze_system/cli/rag.py src/stock_analyze_system/cli/serve.py tests/unit/cli/test_app.py
git commit -m "feat: add CLI entry point, parser builder, and module stubs"
```

---

## Task 3: company.py — 企業管理サブコマンド

**Files:**
- Modify: `src/stock_analyze_system/cli/company.py`
- Create: `tests/unit/cli/test_company_cli.py`

- [ ] **Step 1: Write tests**

```python
# tests/unit/cli/test_company_cli.py
"""company CLI のテスト"""
import argparse
from unittest.mock import AsyncMock, MagicMock

import pytest

from stock_analyze_system.cli.company import handle
from stock_analyze_system.cli.helpers import ServiceContainer


def _make_services(**overrides):
    defaults = {
        "company_service": AsyncMock(),
        "financial_service": AsyncMock(),
        "valuation_service": AsyncMock(),
        "filing_service": AsyncMock(),
        "watchlist_service": AsyncMock(),
        "target_service": AsyncMock(),
        "job_service": AsyncMock(),
        "financial_sync": AsyncMock(),
        "filing_sync": AsyncMock(),
    }
    defaults.update(overrides)
    return ServiceContainer(**defaults)


class TestCompanyRegister:
    async def test_register_us(self, capsys):
        svc = _make_services()
        company = MagicMock()
        company.id = "US_AAPL"
        company.name = "Apple"
        svc.company_service.register_company.return_value = company

        args = argparse.Namespace(
            action="register", json=False,
            name="Apple", market="NASDAQ", ticker="AAPL",
            security_code=None, sector=None, cik=None, edinet_code=None,
        )
        await handle(args, svc)
        out = capsys.readouterr().out
        assert "US_AAPL" in out

    async def test_register_json(self, capsys):
        svc = _make_services()
        company = MagicMock()
        company.id = "US_AAPL"
        company.name = "Apple"
        company.market = "NASDAQ"
        company.ticker = "AAPL"
        company.security_code = None
        svc.company_service.register_company.return_value = company

        args = argparse.Namespace(
            action="register", json=True,
            name="Apple", market="NASDAQ", ticker="AAPL",
            security_code=None, sector=None, cik=None, edinet_code=None,
        )
        await handle(args, svc)
        out = capsys.readouterr().out
        assert '"US_AAPL"' in out


class TestCompanySearch:
    async def test_search_results(self, capsys):
        svc = _make_services()
        c1 = MagicMock()
        c1.id = "US_AAPL"
        c1.name = "Apple"
        c1.ticker = "AAPL"
        c1.market = "NASDAQ"
        svc.company_service.search_companies.return_value = [c1]

        args = argparse.Namespace(action="search", json=False, query="Apple", limit=20)
        await handle(args, svc)
        out = capsys.readouterr().out
        assert "AAPL" in out

    async def test_search_no_results(self, capsys):
        svc = _make_services()
        svc.company_service.search_companies.return_value = []

        args = argparse.Namespace(action="search", json=False, query="zzz", limit=20)
        await handle(args, svc)
        out = capsys.readouterr().out
        assert "No" in out


class TestCompanyShow:
    async def test_show(self, capsys):
        svc = _make_services()
        company = MagicMock()
        company.id = "US_AAPL"
        company.name = "Apple"
        company.ticker = "AAPL"
        company.market = "NASDAQ"
        company.sector = "Technology"
        company.accounting_standard = "US-GAAP"
        company.security_code = None
        company.cik = "0000320193"
        company.edinet_code = None
        svc.company_service.get_company.return_value = company

        args = argparse.Namespace(action="show", json=False, company_id="US_AAPL")
        await handle(args, svc)
        out = capsys.readouterr().out
        assert "Apple" in out

    async def test_show_not_found(self):
        svc = _make_services()
        svc.company_service.get_company.return_value = None

        args = argparse.Namespace(action="show", json=False, company_id="US_XXXX")
        with pytest.raises(SystemExit):
            await handle(args, svc)
```

- [ ] **Step 2: Run tests — verify they fail**

Run: `cd <repo-root> && python3 -m pytest tests/unit/cli/test_company_cli.py -v --no-header 2>&1 | tail -20`
Expected: FAIL

- [ ] **Step 3: Implement company.py**

```python
# src/stock_analyze_system/cli/company.py
"""企業管理サブコマンド"""
from __future__ import annotations

import argparse
import sys
from typing import TYPE_CHECKING

from stock_analyze_system.cli.formatters import format_json, format_table
from stock_analyze_system.cli.helpers import require_company

if TYPE_CHECKING:
    from stock_analyze_system.cli.helpers import ServiceContainer


def register_parser(subparsers: argparse._SubParsersAction) -> None:
    parser = subparsers.add_parser("company", help="企業管理")
    sub = parser.add_subparsers(dest="action")

    reg_p = sub.add_parser("register", help="企業を登録")
    reg_p.add_argument("name", type=str, help="企業名")
    reg_p.add_argument("--market", type=str, required=True,
                       help="市場 (NYSE, NASDAQ, TSE_PRIME, etc.)")
    reg_p.add_argument("--ticker", type=str, default=None, help="ティッカー (US)")
    reg_p.add_argument("--security-code", type=str, default=None, help="証券コード (JP)")
    reg_p.add_argument("--sector", type=str, default=None)
    reg_p.add_argument("--cik", type=str, default=None, help="SEC CIK")
    reg_p.add_argument("--edinet-code", type=str, default=None, help="EDINET code")

    search_p = sub.add_parser("search", help="企業を検索")
    search_p.add_argument("query", type=str, help="検索クエリ")
    search_p.add_argument("--limit", type=int, default=20)

    show_p = sub.add_parser("show", help="企業詳細を表示")
    show_p.add_argument("company_id", type=str, help="企業ID")

    parser.set_defaults(handler=handle)


async def handle(args: argparse.Namespace, services: ServiceContainer) -> None:
    if not getattr(args, "action", None):
        print("Usage: stock-analyze company {register|search|show}")
        sys.exit(1)

    handlers = {
        "register": _handle_register,
        "search": _handle_search,
        "show": _handle_show,
    }
    await handlers[args.action](args, services)


async def _handle_register(args: argparse.Namespace, services: ServiceContainer) -> None:
    data = {
        "name": args.name,
        "market": args.market,
        "ticker": args.ticker,
        "security_code": args.security_code,
        "sector": args.sector,
        "cik": args.cik,
        "edinet_code": args.edinet_code,
    }
    company = await services.company_service.register_company(data)
    if args.json:
        print(format_json({
            "id": company.id, "name": company.name,
            "market": company.market, "ticker": company.ticker,
            "security_code": company.security_code,
        }))
    else:
        print(f"Registered: {company.id} ({company.name})")


async def _handle_search(args: argparse.Namespace, services: ServiceContainer) -> None:
    results = await services.company_service.search_companies(args.query, limit=args.limit)
    if not results:
        print(f"No companies found for '{args.query}'.")
        return
    rows = [
        {"ID": c.id, "Name": c.name, "Ticker": c.ticker, "Market": c.market}
        for c in results
    ]
    if args.json:
        print(format_json(rows))
    else:
        print(format_table(rows))


async def _handle_show(args: argparse.Namespace, services: ServiceContainer) -> None:
    company = await require_company(services.company_service, args.company_id)
    data = {
        "ID": company.id,
        "Name": company.name,
        "Ticker": company.ticker,
        "Security Code": company.security_code,
        "Market": company.market,
        "Sector": company.sector,
        "Accounting Standard": company.accounting_standard,
        "CIK": company.cik,
        "EDINET Code": company.edinet_code,
    }
    if args.json:
        print(format_json(data))
    else:
        for key, value in data.items():
            print(f"  {key:>20s}: {value if value is not None else 'N/A'}")
```

- [ ] **Step 4: Run tests — verify they pass**

Run: `cd <repo-root> && python3 -m pytest tests/unit/cli/test_company_cli.py -v --no-header 2>&1 | tail -20`
Expected: ALL PASSED

- [ ] **Step 5: Commit**

```bash
cd <repo-root>
git add src/stock_analyze_system/cli/company.py tests/unit/cli/test_company_cli.py
git commit -m "feat: add company CLI subcommand (register/search/show)"
```

---

## Task 4: financial.py — 財務データサブコマンド

**Files:**
- Modify: `src/stock_analyze_system/cli/financial.py`
- Create: `tests/unit/cli/test_financial_cli.py`

- [ ] **Step 1: Write tests**

```python
# tests/unit/cli/test_financial_cli.py
"""financial CLI のテスト"""
import argparse
from datetime import date
from unittest.mock import AsyncMock, MagicMock

import pytest

from stock_analyze_system.cli.financial import handle
from stock_analyze_system.cli.helpers import ServiceContainer


def _make_services(**overrides):
    defaults = {
        "company_service": AsyncMock(),
        "financial_service": AsyncMock(),
        "valuation_service": AsyncMock(),
        "filing_service": AsyncMock(),
        "watchlist_service": AsyncMock(),
        "target_service": AsyncMock(),
        "job_service": AsyncMock(),
        "financial_sync": AsyncMock(),
        "filing_sync": AsyncMock(),
    }
    defaults.update(overrides)
    return ServiceContainer(**defaults)


def _mock_fd(**kwargs):
    defaults = {
        "fiscal_year_end": date(2024, 9, 28),
        "period_type": "annual",
        "revenue": 394e9,
        "operating_income": 120e9,
        "net_income": 97e9,
        "total_assets": 353e9,
        "equity": 62e9,
        "total_debt": 111e9,
        "cash": 30e9,
        "ebitda": 130e9,
        "eps": 6.42,
        "fcf": 111e9,
        "cogs": 214e9,
        "inventory": 6.5e9,
        "current_assets": 135e9,
        "current_liabilities": 145e9,
        "tax_expense": 17e9,
        "income_before_tax": 114e9,
        "dividends_paid": 15e9,
        "share_repurchases": 77e9,
        "shares_outstanding": 15.1e9,
        "dps": 0.99,
    }
    defaults.update(kwargs)
    return MagicMock(**defaults)


class TestFinancialShow:
    async def test_show_timeseries(self, capsys):
        svc = _make_services()
        company = MagicMock(id="US_AAPL")
        svc.company_service.get_company.return_value = company

        fd = _mock_fd()
        svc.financial_service.get_timeseries.return_value = [fd]

        args = argparse.Namespace(
            action="show", json=False,
            company_id="US_AAPL", period="annual", years=5,
        )
        await handle(args, svc)
        out = capsys.readouterr().out
        assert "2024-09-28" in out

    async def test_show_no_data(self, capsys):
        svc = _make_services()
        svc.company_service.get_company.return_value = MagicMock(id="US_AAPL")
        svc.financial_service.get_timeseries.return_value = []

        args = argparse.Namespace(
            action="show", json=False,
            company_id="US_AAPL", period="annual", years=5,
        )
        await handle(args, svc)
        out = capsys.readouterr().out
        assert "No" in out


class TestFinancialMetrics:
    async def test_metrics(self, capsys):
        svc = _make_services()
        svc.company_service.get_company.return_value = MagicMock(id="US_AAPL")

        fd = _mock_fd()
        svc.financial_service.get_timeseries.return_value = [fd]
        svc.financial_service.compute_timeseries_metrics.return_value = [{
            "fiscal_year_end": date(2024, 9, 28),
            "period_type": "annual",
            "revenue": 394e9,
            "net_income": 97e9,
            "operating_margin": 0.304,
            "net_margin": 0.246,
            "roe": 1.564,
            "revenue_growth": 0.05,
        }]

        args = argparse.Namespace(
            action="metrics", json=False,
            company_id="US_AAPL", period="annual", years=5,
        )
        await handle(args, svc)
        out = capsys.readouterr().out
        assert "2024-09-28" in out
```

- [ ] **Step 2: Run tests — verify they fail**

Run: `cd <repo-root> && python3 -m pytest tests/unit/cli/test_financial_cli.py -v --no-header 2>&1 | tail -20`
Expected: FAIL

- [ ] **Step 3: Implement financial.py**

```python
# src/stock_analyze_system/cli/financial.py
"""財務データサブコマンド"""
from __future__ import annotations

import argparse
import sys
from typing import TYPE_CHECKING

from stock_analyze_system.cli.formatters import (
    fmt_large,
    fmt_number,
    format_json,
    format_table,
)
from stock_analyze_system.cli.helpers import require_company

if TYPE_CHECKING:
    from stock_analyze_system.cli.helpers import ServiceContainer

_PCT_KEYS = frozenset({
    "operating_margin", "net_margin", "roe", "roa", "roic",
    "equity_ratio", "revenue_growth", "eps_growth", "fcf_growth",
    "dividend_payout_ratio", "total_payout_ratio",
})


def _fmt_metric(key: str, val: float | None) -> str:
    if val is None:
        return "N/A"
    if key in _PCT_KEYS:
        return f"{val * 100:.2f}%"
    return fmt_number(val)


def register_parser(subparsers: argparse._SubParsersAction) -> None:
    parser = subparsers.add_parser("financial", help="財務データ表示")
    sub = parser.add_subparsers(dest="action")

    show_p = sub.add_parser("show", help="財務データ一覧")
    show_p.add_argument("company_id", type=str)
    show_p.add_argument("--period", type=str, default="annual",
                        choices=["annual", "quarterly"])
    show_p.add_argument("--years", type=int, default=5)

    metrics_p = sub.add_parser("metrics", help="財務指標表示")
    metrics_p.add_argument("company_id", type=str)
    metrics_p.add_argument("--period", type=str, default="annual",
                           choices=["annual", "quarterly"])
    metrics_p.add_argument("--years", type=int, default=5)

    parser.set_defaults(handler=handle)


async def handle(args: argparse.Namespace, services: ServiceContainer) -> None:
    if not getattr(args, "action", None):
        print("Usage: stock-analyze financial {show|metrics}")
        sys.exit(1)

    handlers = {
        "show": _handle_show,
        "metrics": _handle_metrics,
    }
    await handlers[args.action](args, services)


async def _handle_show(args: argparse.Namespace, services: ServiceContainer) -> None:
    await require_company(services.company_service, args.company_id)
    data = await services.financial_service.get_timeseries(
        args.company_id, args.period, years=args.years,
    )
    if not data:
        print(f"No financial data for '{args.company_id}'.")
        return

    rows = [
        {
            "Period End": str(fd.fiscal_year_end),
            "Type": fd.period_type,
            "Revenue": fmt_large(fd.revenue),
            "Op Income": fmt_large(fd.operating_income),
            "Net Income": fmt_large(fd.net_income),
            "EPS": fmt_number(fd.eps),
            "EBITDA": fmt_large(fd.ebitda),
        }
        for fd in data
    ]
    if args.json:
        print(format_json(rows))
    else:
        print(f"Financial Data: {args.company_id} ({args.period})")
        print(format_table(rows))


async def _handle_metrics(args: argparse.Namespace, services: ServiceContainer) -> None:
    await require_company(services.company_service, args.company_id)
    ts_data = await services.financial_service.get_timeseries(
        args.company_id, args.period, years=args.years,
    )
    if not ts_data:
        print(f"No financial data for '{args.company_id}'.")
        return

    metrics_list = services.financial_service.compute_timeseries_metrics(ts_data)
    rows = [
        {
            "Period End": str(m["fiscal_year_end"]),
            "Op Margin": _fmt_metric("operating_margin", m.get("operating_margin")),
            "Net Margin": _fmt_metric("net_margin", m.get("net_margin")),
            "ROE": _fmt_metric("roe", m.get("roe")),
            "Rev Growth": _fmt_metric("revenue_growth", m.get("revenue_growth")),
            "EPS Growth": _fmt_metric("eps_growth", m.get("eps_growth")),
        }
        for m in metrics_list
    ]
    if args.json:
        print(format_json(metrics_list))
    else:
        print(f"Financial Metrics: {args.company_id} ({args.period})")
        print(format_table(rows))
```

- [ ] **Step 4: Run tests — verify they pass**

Run: `cd <repo-root> && python3 -m pytest tests/unit/cli/test_financial_cli.py -v --no-header 2>&1 | tail -20`
Expected: ALL PASSED

- [ ] **Step 5: Commit**

```bash
cd <repo-root>
git add src/stock_analyze_system/cli/financial.py tests/unit/cli/test_financial_cli.py
git commit -m "feat: add financial CLI subcommand (show/metrics)"
```

---

## Task 5: valuation.py — バリュエーションサブコマンド (Bug #16 修正)

**Files:**
- Modify: `src/stock_analyze_system/cli/valuation.py`
- Create: `tests/unit/cli/test_valuation_cli.py`

Bug #16 修正: 手動 usage 文字列を使わず、argparse の自動生成ヘルプに委ねる。`deviation` サブコマンドも自然に表示される。

- [ ] **Step 1: Write tests**

```python
# tests/unit/cli/test_valuation_cli.py
"""valuation CLI のテスト (Bug #16 修正含む)"""
import argparse
from datetime import date
from unittest.mock import AsyncMock, MagicMock

import pytest

from stock_analyze_system.cli.valuation import handle, register_parser
from stock_analyze_system.cli.helpers import ServiceContainer


def _make_services(**overrides):
    defaults = {
        "company_service": AsyncMock(),
        "financial_service": AsyncMock(),
        "valuation_service": AsyncMock(),
        "filing_service": AsyncMock(),
        "watchlist_service": AsyncMock(),
        "target_service": AsyncMock(),
        "job_service": AsyncMock(),
        "financial_sync": AsyncMock(),
        "filing_sync": AsyncMock(),
    }
    defaults.update(overrides)
    return ServiceContainer(**defaults)


class TestBug16DeviationInHelp:
    """Bug #16: deviation がヘルプに含まれること"""

    def test_deviation_subcommand_registered(self):
        """deviation サブコマンドが登録されていること"""
        parser = argparse.ArgumentParser()
        sub = parser.add_subparsers(dest="command")
        register_parser(sub)
        # deviation がパースできること
        args = parser.parse_args(["valuation", "deviation", "US_AAPL", "US_MSFT"])
        assert args.action == "deviation"

    def test_no_manual_usage_string(self):
        """手動usage文字列が使われていないこと（argparse自動生成に委任）"""
        import inspect
        from stock_analyze_system.cli import valuation
        source = inspect.getsource(valuation)
        # "Usage:" 手動文字列がないこと
        assert "Usage: stock-analyze valuation {" not in source


class TestValuationShow:
    async def test_show(self, capsys):
        svc = _make_services()
        svc.company_service.get_company.return_value = MagicMock(id="US_AAPL")

        v = MagicMock()
        v.date = date(2024, 6, 1)
        v.stock_price = 210.0
        v.market_cap = 3.2e12
        v.per = 32.0
        v.pbr = 48.0
        v.ev_ebitda = 25.0
        v.psr = 8.0
        v.fcf_yield = 0.035
        svc.valuation_service.get_history.return_value = [v]

        args = argparse.Namespace(
            action="show", json=False, company_id="US_AAPL", years=5,
        )
        await handle(args, svc)
        out = capsys.readouterr().out
        assert "2024-06-01" in out
        assert "32.00" in out


class TestValuationCompare:
    async def test_compare(self, capsys):
        svc = _make_services()
        svc.company_service.get_company.return_value = MagicMock()
        svc.valuation_service.compare_valuations.return_value = [
            {"company_id": "US_AAPL", "per": 32.0, "pbr": 48.0,
             "ev_ebitda": 25.0, "psr": 8.0, "date": date(2024, 6, 1),
             "stock_price": 210.0, "market_cap": 3.2e12, "fcf_yield": 0.035},
            {"company_id": "US_MSFT", "per": 35.0, "pbr": 13.0,
             "ev_ebitda": 22.0, "psr": 12.0, "date": date(2024, 6, 1),
             "stock_price": 430.0, "market_cap": 3.1e12, "fcf_yield": 0.03},
        ]

        args = argparse.Namespace(
            action="compare", json=False,
            company_ids=["US_AAPL", "US_MSFT"],
        )
        await handle(args, svc)
        out = capsys.readouterr().out
        assert "US_AAPL" in out
        assert "US_MSFT" in out


class TestValuationRange:
    async def test_range(self, capsys):
        svc = _make_services()
        svc.company_service.get_company.return_value = MagicMock()
        svc.valuation_service.get_history.return_value = [MagicMock()]
        svc.valuation_service.compute_per_range.return_value = {
            "high": 40.0, "median": 28.0, "low": 18.0,
        }

        args = argparse.Namespace(
            action="range", json=False, company_id="US_AAPL",
        )
        await handle(args, svc)
        out = capsys.readouterr().out
        assert "40.00" in out
        assert "28.00" in out
        assert "18.00" in out


class TestValuationDeviation:
    async def test_deviation(self, capsys):
        svc = _make_services()
        svc.company_service.get_company.return_value = MagicMock()
        svc.valuation_service.compare_valuations.return_value = [
            {"company_id": "US_AAPL", "per": 32.0},
            {"company_id": "US_MSFT", "per": 35.0},
        ]
        svc.valuation_service.compute_group_deviation.return_value = [
            {"company_id": "US_AAPL", "per": 32.0, "per_zscore": -0.71,
             "pbr": None, "pbr_zscore": None,
             "ev_ebitda": None, "ev_ebitda_zscore": None,
             "psr": None, "psr_zscore": None},
            {"company_id": "US_MSFT", "per": 35.0, "per_zscore": 0.71,
             "pbr": None, "pbr_zscore": None,
             "ev_ebitda": None, "ev_ebitda_zscore": None,
             "psr": None, "psr_zscore": None},
        ]

        args = argparse.Namespace(
            action="deviation", json=False,
            company_ids=["US_AAPL", "US_MSFT"],
        )
        await handle(args, svc)
        out = capsys.readouterr().out
        assert "US_AAPL" in out

    async def test_deviation_too_few(self):
        svc = _make_services()
        args = argparse.Namespace(
            action="deviation", json=False,
            company_ids=["US_AAPL"],
        )
        with pytest.raises(SystemExit):
            await handle(args, svc)
```

- [ ] **Step 2: Run tests — verify they fail**

Run: `cd <repo-root> && python3 -m pytest tests/unit/cli/test_valuation_cli.py -v --no-header 2>&1 | tail -20`
Expected: FAIL

- [ ] **Step 3: Implement valuation.py**

```python
# src/stock_analyze_system/cli/valuation.py
"""バリュエーションサブコマンド (Bug #16修正: argparse自動ヘルプ)"""
from __future__ import annotations

import argparse
import sys
from typing import TYPE_CHECKING

from stock_analyze_system.cli.formatters import (
    fmt_large,
    fmt_number,
    format_json,
    format_table,
)
from stock_analyze_system.cli.helpers import require_company

if TYPE_CHECKING:
    from stock_analyze_system.cli.helpers import ServiceContainer


def register_parser(subparsers: argparse._SubParsersAction) -> None:
    parser = subparsers.add_parser("valuation", help="バリュエーション表示")
    sub = parser.add_subparsers(dest="action")

    show_p = sub.add_parser("show", help="バリュエーション履歴")
    show_p.add_argument("company_id", type=str)
    show_p.add_argument("--years", type=int, default=5)

    compare_p = sub.add_parser("compare", help="複数企業比較")
    compare_p.add_argument("company_ids", nargs="+", type=str)

    range_p = sub.add_parser("range", help="PERレンジ (高値/中央値/安値)")
    range_p.add_argument("company_id", type=str)

    dev_p = sub.add_parser("deviation", help="グループ偏差分析 (z-score)")
    dev_p.add_argument("company_ids", nargs="+", type=str)

    parser.set_defaults(handler=handle)


async def handle(args: argparse.Namespace, services: ServiceContainer) -> None:
    # Bug #16修正: 手動usage文字列を使わない。argparseの自動ヘルプに委任。
    if not getattr(args, "action", None):
        # argparse が自動でヘルプを出すが、フォールバックとして
        sys.exit(1)

    handlers = {
        "show": _handle_show,
        "compare": _handle_compare,
        "range": _handle_range,
        "deviation": _handle_deviation,
    }
    await handlers[args.action](args, services)


async def _handle_show(args: argparse.Namespace, services: ServiceContainer) -> None:
    await require_company(services.company_service, args.company_id)
    valuations = await services.valuation_service.get_history(
        args.company_id, years=args.years,
    )
    if not valuations:
        print(f"No valuation data for '{args.company_id}'.")
        return

    rows = [_valuation_to_row(v) for v in valuations]
    if args.json:
        print(format_json(rows))
    else:
        print(f"Valuation: {args.company_id}")
        print(format_table(rows))


async def _handle_compare(args: argparse.Namespace, services: ServiceContainer) -> None:
    for cid in args.company_ids:
        await require_company(services.company_service, cid)

    comparisons = await services.valuation_service.compare_valuations(args.company_ids)
    rows = [
        {
            "Company": r["company_id"],
            "Date": str(r.get("date", "N/A")),
            "Price": fmt_number(r.get("stock_price")),
            "PER": fmt_number(r.get("per")),
            "PBR": fmt_number(r.get("pbr")),
            "EV/EBITDA": fmt_number(r.get("ev_ebitda")),
            "PSR": fmt_number(r.get("psr")),
        }
        for r in comparisons
    ]
    if args.json:
        print(format_json(comparisons))
    else:
        print("Valuation Comparison (latest)")
        print(format_table(rows))


async def _handle_range(args: argparse.Namespace, services: ServiceContainer) -> None:
    await require_company(services.company_service, args.company_id)
    valuations = await services.valuation_service.get_history(args.company_id, years=10)
    if not valuations:
        print(f"No valuation data for '{args.company_id}'.")
        return

    per_range = services.valuation_service.compute_per_range(valuations)
    if per_range["high"] is None:
        print("No PER data available.")
        return

    if args.json:
        print(format_json({"company_id": args.company_id, **per_range}))
    else:
        print(f"PER Range: {args.company_id}")
        print(f"  High:   {per_range['high']:.2f}")
        print(f"  Median: {per_range['median']:.2f}")
        print(f"  Low:    {per_range['low']:.2f}")


async def _handle_deviation(args: argparse.Namespace, services: ServiceContainer) -> None:
    if len(args.company_ids) < 2:
        print("At least 2 companies required for deviation analysis.",
              file=sys.stderr)
        sys.exit(1)

    for cid in args.company_ids:
        await require_company(services.company_service, cid)

    comparisons = await services.valuation_service.compare_valuations(args.company_ids)
    results = services.valuation_service.compute_group_deviation(comparisons)

    rows = []
    for r in results:
        row = {"Company": r["company_id"]}
        for metric in ("per", "pbr", "ev_ebitda", "psr"):
            val = r.get(metric)
            z = r.get(f"{metric}_zscore")
            val_str = fmt_number(val)
            z_str = f"({z:+.2f}σ)" if z is not None else ""
            row[metric.upper().replace("_", "/")] = f"{val_str} {z_str}".strip()
        rows.append(row)

    if args.json:
        print(format_json(results))
    else:
        print(f"Valuation Deviation (n={len(args.company_ids)})")
        print(format_table(rows))


def _valuation_to_row(v) -> dict:
    return {
        "Date": str(v.date),
        "Price": fmt_number(v.stock_price),
        "Market Cap": fmt_large(v.market_cap),
        "PER": fmt_number(v.per),
        "PBR": fmt_number(v.pbr),
        "EV/EBITDA": fmt_number(v.ev_ebitda),
        "PSR": fmt_number(v.psr),
        "FCF Yield": fmt_number(v.fcf_yield),
    }
```

- [ ] **Step 4: Run tests — verify they pass**

Run: `cd <repo-root> && python3 -m pytest tests/unit/cli/test_valuation_cli.py -v --no-header 2>&1 | tail -20`
Expected: ALL PASSED

- [ ] **Step 5: Commit**

```bash
cd <repo-root>
git add src/stock_analyze_system/cli/valuation.py tests/unit/cli/test_valuation_cli.py
git commit -m "feat: add valuation CLI subcommand with Bug #16 fix (deviation in help)"
```

---

## Task 6: filings.py — ファイリングサブコマンド

**Files:**
- Modify: `src/stock_analyze_system/cli/filings.py`
- Create: `tests/unit/cli/test_filings_cli.py`

- [ ] **Step 1: Write tests**

```python
# tests/unit/cli/test_filings_cli.py
"""filings CLI のテスト"""
import argparse
from datetime import date
from unittest.mock import AsyncMock, MagicMock

import pytest

from stock_analyze_system.cli.filings import handle
from stock_analyze_system.cli.helpers import ServiceContainer


def _make_services(**overrides):
    defaults = {
        "company_service": AsyncMock(),
        "financial_service": AsyncMock(),
        "valuation_service": AsyncMock(),
        "filing_service": AsyncMock(),
        "watchlist_service": AsyncMock(),
        "target_service": AsyncMock(),
        "job_service": AsyncMock(),
        "financial_sync": AsyncMock(),
        "filing_sync": AsyncMock(),
    }
    defaults.update(overrides)
    return ServiceContainer(**defaults)


class TestFilingsList:
    async def test_list(self, capsys):
        svc = _make_services()
        svc.company_service.get_company.return_value = MagicMock(id="US_AAPL")

        f1 = MagicMock()
        f1.id = 1
        f1.filing_type = "10-K"
        f1.source = "SEC"
        f1.fiscal_year = 2024
        f1.period_end = date(2024, 9, 28)
        f1.filed_at = date(2024, 11, 1)
        svc.filing_service.list_filings.return_value = [f1]

        args = argparse.Namespace(
            action="list", json=False, company_id="US_AAPL",
        )
        await handle(args, svc)
        out = capsys.readouterr().out
        assert "10-K" in out

    async def test_list_empty(self, capsys):
        svc = _make_services()
        svc.company_service.get_company.return_value = MagicMock(id="US_AAPL")
        svc.filing_service.list_filings.return_value = []

        args = argparse.Namespace(
            action="list", json=False, company_id="US_AAPL",
        )
        await handle(args, svc)
        out = capsys.readouterr().out
        assert "No" in out


class TestFilingsDownload:
    async def test_download_delegates_to_sync(self, capsys):
        svc = _make_services()
        company = MagicMock(id="US_AAPL", cik="0000320193", edinet_code=None)
        svc.company_service.get_company.return_value = company
        svc.filing_sync.update_from_sec.return_value = 3

        args = argparse.Namespace(
            action="download", json=False, company_id="US_AAPL",
        )
        await handle(args, svc)
        svc.filing_sync.update_from_sec.assert_called_once()
        out = capsys.readouterr().out
        assert "3" in out
```

- [ ] **Step 2: Run tests — verify they fail**

Run: `cd <repo-root> && python3 -m pytest tests/unit/cli/test_filings_cli.py -v --no-header 2>&1 | tail -20`
Expected: FAIL

- [ ] **Step 3: Implement filings.py**

```python
# src/stock_analyze_system/cli/filings.py
"""ファイリングサブコマンド"""
from __future__ import annotations

import argparse
import sys
from typing import TYPE_CHECKING

from stock_analyze_system.cli.formatters import format_json, format_table
from stock_analyze_system.cli.helpers import require_company

if TYPE_CHECKING:
    from stock_analyze_system.cli.helpers import ServiceContainer


def register_parser(subparsers: argparse._SubParsersAction) -> None:
    parser = subparsers.add_parser("filings", help="ファイリング管理")
    sub = parser.add_subparsers(dest="action")

    list_p = sub.add_parser("list", help="ファイリング一覧")
    list_p.add_argument("company_id", type=str)

    dl_p = sub.add_parser("download", help="ファイリングをダウンロード")
    dl_p.add_argument("company_id", type=str)

    parser.set_defaults(handler=handle)


async def handle(args: argparse.Namespace, services: ServiceContainer) -> None:
    if not getattr(args, "action", None):
        sys.exit(1)

    handlers = {
        "list": _handle_list,
        "download": _handle_download,
    }
    await handlers[args.action](args, services)


async def _handle_list(args: argparse.Namespace, services: ServiceContainer) -> None:
    await require_company(services.company_service, args.company_id)
    filings = await services.filing_service.list_filings(args.company_id)
    if not filings:
        print(f"No filings found for '{args.company_id}'.")
        return

    rows = [
        {
            "ID": f.id,
            "Type": f.filing_type,
            "Source": f.source,
            "FY": f.fiscal_year,
            "Period End": str(f.period_end) if f.period_end else "N/A",
            "Filed At": str(f.filed_at) if f.filed_at else "N/A",
        }
        for f in filings
    ]
    if args.json:
        print(format_json(rows))
    else:
        print(f"Filings: {args.company_id}")
        print(format_table(rows))


async def _handle_download(args: argparse.Namespace, services: ServiceContainer) -> None:
    company = await require_company(services.company_service, args.company_id)

    count = 0
    if company.cik:
        count = await services.filing_sync.update_from_sec(
            args.company_id, company.cik,
        )
    elif company.edinet_code:
        count = await services.filing_sync.update_from_edinet(
            args.company_id, company.edinet_code,
        )
    else:
        print(f"No CIK or EDINET code for '{args.company_id}'. Cannot download filings.",
              file=sys.stderr)
        sys.exit(1)

    print(f"Downloaded {count} filing(s) for '{args.company_id}'.")
```

- [ ] **Step 4: Run tests — verify they pass**

Run: `cd <repo-root> && python3 -m pytest tests/unit/cli/test_filings_cli.py -v --no-header 2>&1 | tail -20`
Expected: ALL PASSED

- [ ] **Step 5: Commit**

```bash
cd <repo-root>
git add src/stock_analyze_system/cli/filings.py tests/unit/cli/test_filings_cli.py
git commit -m "feat: add filings CLI subcommand (list/download)"
```

---

## Task 7: jobs.py — ジョブサブコマンド (Bug #3 修正)

**Files:**
- Modify: `src/stock_analyze_system/cli/jobs.py`
- Create: `tests/unit/cli/test_jobs_cli.py`

Bug #3 修正: `--type` オプションを削除。`sync <company_id>` と `daily --market` の2つのサブコマンドに分離。

- [ ] **Step 1: Write tests**

```python
# tests/unit/cli/test_jobs_cli.py
"""jobs CLI のテスト (Bug #3 修正含む)"""
import argparse
from unittest.mock import AsyncMock, MagicMock

import pytest

from stock_analyze_system.cli.jobs import handle, register_parser
from stock_analyze_system.cli.helpers import ServiceContainer
from stock_analyze_system.services.job import DailyUpdateResult, SyncResult


def _make_services(**overrides):
    defaults = {
        "company_service": AsyncMock(),
        "financial_service": AsyncMock(),
        "valuation_service": AsyncMock(),
        "filing_service": AsyncMock(),
        "watchlist_service": AsyncMock(),
        "target_service": AsyncMock(),
        "job_service": AsyncMock(),
        "financial_sync": AsyncMock(),
        "filing_sync": AsyncMock(),
    }
    defaults.update(overrides)
    return ServiceContainer(**defaults)


class TestBug3NoTypeOption:
    """Bug #3: --type オプションが存在しないこと"""

    def test_no_type_argument(self):
        """--type 引数が定義されていないこと"""
        parser = argparse.ArgumentParser()
        sub = parser.add_subparsers(dest="command")
        register_parser(sub)

        # --type を渡すとエラーになること
        with pytest.raises(SystemExit):
            parser.parse_args(["jobs", "sync", "US_AAPL", "--type", "daily"])

    def test_sync_subcommand(self):
        parser = argparse.ArgumentParser()
        sub = parser.add_subparsers(dest="command")
        register_parser(sub)
        args = parser.parse_args(["jobs", "sync", "US_AAPL"])
        assert args.action == "sync"
        assert args.company_id == "US_AAPL"

    def test_daily_subcommand(self):
        parser = argparse.ArgumentParser()
        sub = parser.add_subparsers(dest="command")
        register_parser(sub)
        args = parser.parse_args(["jobs", "daily", "--market", "us"])
        assert args.action == "daily"
        assert args.market == "us"


class TestJobsSync:
    async def test_sync(self, capsys):
        svc = _make_services()
        result = SyncResult(
            company_id="US_AAPL",
            financials_count=5,
            filings_count=3,
            valuations_count=1,
        )
        svc.job_service.sync_company.return_value = result

        args = argparse.Namespace(action="sync", json=False, company_id="US_AAPL")
        await handle(args, svc)
        out = capsys.readouterr().out
        assert "US_AAPL" in out
        assert "5" in out


class TestJobsDaily:
    async def test_daily(self, capsys):
        svc = _make_services()
        result = DailyUpdateResult(market="us", total_companies=2)
        result.results = [
            SyncResult(company_id="US_AAPL", financials_count=5),
            SyncResult(company_id="US_MSFT", financials_count=3),
        ]
        svc.job_service.run_daily_update.return_value = result

        args = argparse.Namespace(action="daily", json=False, market="us")
        await handle(args, svc)
        out = capsys.readouterr().out
        assert "us" in out.lower() or "US" in out
```

- [ ] **Step 2: Run tests — verify they fail**

Run: `cd <repo-root> && python3 -m pytest tests/unit/cli/test_jobs_cli.py -v --no-header 2>&1 | tail -20`
Expected: FAIL

- [ ] **Step 3: Implement jobs.py**

```python
# src/stock_analyze_system/cli/jobs.py
"""ジョブサブコマンド (Bug #3修正: --type削除, sync/daily分離)"""
from __future__ import annotations

import argparse
import sys
from typing import TYPE_CHECKING

from stock_analyze_system.cli.formatters import format_json

if TYPE_CHECKING:
    from stock_analyze_system.cli.helpers import ServiceContainer


def register_parser(subparsers: argparse._SubParsersAction) -> None:
    parser = subparsers.add_parser("jobs", help="ジョブ管理")
    sub = parser.add_subparsers(dest="action")

    # Bug #3修正: sync と daily を明確に分離。--type は削除。
    sync_p = sub.add_parser("sync", help="単一企業のデータ同期")
    sync_p.add_argument("company_id", type=str, help="企業ID")

    daily_p = sub.add_parser("daily", help="日次更新バッチ")
    daily_p.add_argument(
        "--market", type=str, choices=["us", "jp"],
        default="us", help="対象市場",
    )

    parser.set_defaults(handler=handle)


async def handle(args: argparse.Namespace, services: ServiceContainer) -> None:
    if not getattr(args, "action", None):
        sys.exit(1)

    handlers = {
        "sync": _handle_sync,
        "daily": _handle_daily,
    }
    await handlers[args.action](args, services)


async def _handle_sync(args: argparse.Namespace, services: ServiceContainer) -> None:
    print(f"Syncing data for '{args.company_id}'...")
    result = await services.job_service.sync_company(args.company_id)

    if args.json:
        print(format_json({
            "company_id": result.company_id,
            "financials_count": result.financials_count,
            "filings_count": result.filings_count,
            "valuations_count": result.valuations_count,
            "errors": result.errors,
        }))
    else:
        print(f"  Company:    {result.company_id}")
        print(f"  Financials: {result.financials_count}")
        print(f"  Filings:    {result.filings_count}")
        print(f"  Valuations: {result.valuations_count}")
        if result.errors:
            print(f"  Errors:     {', '.join(result.errors)}")
        print("Sync complete.")


async def _handle_daily(args: argparse.Namespace, services: ServiceContainer) -> None:
    print(f"Running daily update for market: {args.market.upper()}...")
    result = await services.job_service.run_daily_update(market=args.market)

    if args.json:
        print(format_json({
            "market": result.market,
            "total_companies": result.total_companies,
            "results": [
                {
                    "company_id": r.company_id,
                    "financials_count": r.financials_count,
                    "filings_count": r.filings_count,
                    "valuations_count": r.valuations_count,
                    "errors": r.errors,
                }
                for r in result.results
            ],
        }))
    else:
        print(f"  Total companies: {result.total_companies}")
        success = sum(1 for r in result.results if not r.errors)
        failed = sum(1 for r in result.results if r.errors)
        print(f"  Succeeded: {success}")
        print(f"  Failed:    {failed}")
        if failed:
            for r in result.results:
                if r.errors:
                    print(f"    {r.company_id}: {', '.join(r.errors)}")
        print("Daily update complete.")
```

- [ ] **Step 4: Run tests — verify they pass**

Run: `cd <repo-root> && python3 -m pytest tests/unit/cli/test_jobs_cli.py -v --no-header 2>&1 | tail -20`
Expected: ALL PASSED

- [ ] **Step 5: Commit**

```bash
cd <repo-root>
git add src/stock_analyze_system/cli/jobs.py tests/unit/cli/test_jobs_cli.py
git commit -m "feat: add jobs CLI subcommand with Bug #3 fix (remove --type, add sync/daily)"
```

---

## Task 8: watchlist.py — ウォッチリストサブコマンド (Bug #17 修正)

**Files:**
- Modify: `src/stock_analyze_system/cli/watchlist.py`
- Create: `tests/unit/cli/test_watchlist_cli.py`

Bug #17 修正: 全ハンドラを `async def handler(args, services)` に統一。

- [ ] **Step 1: Write tests**

```python
# tests/unit/cli/test_watchlist_cli.py
"""watchlist CLI のテスト (Bug #17 修正含む)"""
import argparse
from unittest.mock import AsyncMock, MagicMock

import pytest

from stock_analyze_system.cli.watchlist import handle
from stock_analyze_system.cli.helpers import ServiceContainer
from stock_analyze_system.exceptions import DuplicateError, NotFoundError


def _make_services(**overrides):
    defaults = {
        "company_service": AsyncMock(),
        "financial_service": AsyncMock(),
        "valuation_service": AsyncMock(),
        "filing_service": AsyncMock(),
        "watchlist_service": AsyncMock(),
        "target_service": AsyncMock(),
        "job_service": AsyncMock(),
        "financial_sync": AsyncMock(),
        "filing_sync": AsyncMock(),
    }
    defaults.update(overrides)
    return ServiceContainer(**defaults)


class TestBug17HandlerSignature:
    """Bug #17: 全ハンドラが services を受け取ること"""

    async def test_create_uses_services(self, capsys):
        svc = _make_services()
        wl = MagicMock()
        wl.id = 1
        wl.name = "My List"
        svc.watchlist_service.create_watchlist.return_value = wl

        args = argparse.Namespace(
            action="create", json=False,
            name="My List", description=None,
        )
        await handle(args, svc)
        svc.watchlist_service.create_watchlist.assert_called_once()

    async def test_list_uses_services(self, capsys):
        svc = _make_services()
        wl = MagicMock()
        wl.id = 1
        wl.name = "My List"
        wl.description = "Test"
        svc.watchlist_service.list_watchlists.return_value = [wl]

        args = argparse.Namespace(action="list", json=False)
        await handle(args, svc)
        out = capsys.readouterr().out
        assert "My List" in out


class TestWatchlistCreate:
    async def test_create(self, capsys):
        svc = _make_services()
        wl = MagicMock()
        wl.id = 1
        wl.name = "Growth"
        svc.watchlist_service.create_watchlist.return_value = wl

        args = argparse.Namespace(
            action="create", json=False,
            name="Growth", description="Growth stocks",
        )
        await handle(args, svc)
        out = capsys.readouterr().out
        assert "Growth" in out

    async def test_create_duplicate(self, capsys):
        svc = _make_services()
        svc.watchlist_service.create_watchlist.side_effect = DuplicateError("exists")

        args = argparse.Namespace(
            action="create", json=False,
            name="Growth", description=None,
        )
        with pytest.raises(SystemExit):
            await handle(args, svc)


class TestWatchlistShow:
    async def test_show(self, capsys):
        svc = _make_services()
        wl = MagicMock()
        wl.id = 1
        wl.name = "My List"
        wl.description = "Test"
        svc.watchlist_service.get_watchlist.return_value = wl

        item = MagicMock()
        item.company_id = "US_AAPL"
        item.status = "monitoring"
        svc.watchlist_service.list_items = AsyncMock(return_value=[item])

        args = argparse.Namespace(action="show", json=False, watchlist_id=1)
        await handle(args, svc)
        out = capsys.readouterr().out
        assert "US_AAPL" in out


class TestWatchlistAddRemove:
    async def test_add(self, capsys):
        svc = _make_services()
        svc.watchlist_service.add_item.return_value = MagicMock()

        args = argparse.Namespace(
            action="add", json=False,
            watchlist_id=1, company_id="US_AAPL",
        )
        await handle(args, svc)
        svc.watchlist_service.add_item.assert_called_once_with(1, "US_AAPL")

    async def test_remove(self, capsys):
        svc = _make_services()

        args = argparse.Namespace(
            action="remove", json=False,
            watchlist_id=1, company_id="US_AAPL",
        )
        await handle(args, svc)
        svc.watchlist_service.remove_item.assert_called_once_with(1, "US_AAPL")

    async def test_remove_not_found(self):
        svc = _make_services()
        svc.watchlist_service.remove_item.side_effect = NotFoundError("not found")

        args = argparse.Namespace(
            action="remove", json=False,
            watchlist_id=1, company_id="US_XXXX",
        )
        with pytest.raises(SystemExit):
            await handle(args, svc)
```

- [ ] **Step 2: Run tests — verify they fail**

Run: `cd <repo-root> && python3 -m pytest tests/unit/cli/test_watchlist_cli.py -v --no-header 2>&1 | tail -20`
Expected: FAIL

- [ ] **Step 3: Implement watchlist.py**

```python
# src/stock_analyze_system/cli/watchlist.py
"""ウォッチリストサブコマンド (Bug #17修正: 全ハンドラ統一署名)"""
from __future__ import annotations

import argparse
import sys
from typing import TYPE_CHECKING

from stock_analyze_system.cli.formatters import format_json, format_table
from stock_analyze_system.exceptions import DuplicateError, NotFoundError

if TYPE_CHECKING:
    from stock_analyze_system.cli.helpers import ServiceContainer


def register_parser(subparsers: argparse._SubParsersAction) -> None:
    parser = subparsers.add_parser("watchlist", help="ウォッチリスト管理")
    sub = parser.add_subparsers(dest="action")

    create_p = sub.add_parser("create", help="ウォッチリスト作成")
    create_p.add_argument("name", type=str, help="ウォッチリスト名")
    create_p.add_argument("--description", type=str, default=None)

    sub.add_parser("list", help="ウォッチリスト一覧")

    show_p = sub.add_parser("show", help="ウォッチリスト詳細")
    show_p.add_argument("watchlist_id", type=int)

    add_p = sub.add_parser("add", help="銘柄を追加")
    add_p.add_argument("watchlist_id", type=int)
    add_p.add_argument("company_id", type=str)

    rm_p = sub.add_parser("remove", help="銘柄を削除")
    rm_p.add_argument("watchlist_id", type=int)
    rm_p.add_argument("company_id", type=str)

    parser.set_defaults(handler=handle)


async def handle(args: argparse.Namespace, services: ServiceContainer) -> None:
    """Bug #17修正: 全ハンドラが (args, services) を受け取る"""
    if not getattr(args, "action", None):
        sys.exit(1)

    handlers = {
        "create": _handle_create,
        "list": _handle_list,
        "show": _handle_show,
        "add": _handle_add,
        "remove": _handle_remove,
    }
    await handlers[args.action](args, services)


async def _handle_create(args: argparse.Namespace, services: ServiceContainer) -> None:
    try:
        wl = await services.watchlist_service.create_watchlist(
            args.name, description=args.description,
        )
    except DuplicateError as e:
        print(str(e), file=sys.stderr)
        sys.exit(1)

    if args.json:
        print(format_json({"id": wl.id, "name": wl.name}))
    else:
        print(f"Created watchlist: {wl.name} (id={wl.id})")


async def _handle_list(args: argparse.Namespace, services: ServiceContainer) -> None:
    watchlists = await services.watchlist_service.list_watchlists()
    if not watchlists:
        print("No watchlists found.")
        return

    rows = [
        {"ID": wl.id, "Name": wl.name, "Description": wl.description or ""}
        for wl in watchlists
    ]
    if args.json:
        print(format_json(rows))
    else:
        print(format_table(rows))


async def _handle_show(args: argparse.Namespace, services: ServiceContainer) -> None:
    wl = await services.watchlist_service.get_watchlist(args.watchlist_id)
    if wl is None:
        print(f"Watchlist {args.watchlist_id} not found.", file=sys.stderr)
        sys.exit(1)

    items = await services.watchlist_service.list_items(args.watchlist_id)
    rows = [
        {"Company": item.company_id, "Status": item.status}
        for item in items
    ]

    if args.json:
        print(format_json({
            "id": wl.id, "name": wl.name,
            "description": wl.description, "items": rows,
        }))
    else:
        print(f"Watchlist: {wl.name} (id={wl.id})")
        if rows:
            print(format_table(rows))
        else:
            print("  (empty)")


async def _handle_add(args: argparse.Namespace, services: ServiceContainer) -> None:
    try:
        await services.watchlist_service.add_item(
            args.watchlist_id, args.company_id,
        )
    except (NotFoundError, DuplicateError) as e:
        print(str(e), file=sys.stderr)
        sys.exit(1)
    print(f"Added '{args.company_id}' to watchlist {args.watchlist_id}.")


async def _handle_remove(args: argparse.Namespace, services: ServiceContainer) -> None:
    try:
        await services.watchlist_service.remove_item(
            args.watchlist_id, args.company_id,
        )
    except NotFoundError as e:
        print(str(e), file=sys.stderr)
        sys.exit(1)
    print(f"Removed '{args.company_id}' from watchlist {args.watchlist_id}.")
```

**注意**: `list_items` は `WatchlistService` に存在しないため、追加が必要。`WatchlistRepository.list_items` は既に存在する。`WatchlistService` に `list_items` メソッドを追加する:

```python
# src/stock_analyze_system/services/watchlist.py に追加:
    async def list_items(self, watchlist_id: int):
        return await self._repo.list_items(watchlist_id)
```

- [ ] **Step 4: Run tests — verify they pass**

Run: `cd <repo-root> && python3 -m pytest tests/unit/cli/test_watchlist_cli.py -v --no-header 2>&1 | tail -20`
Expected: ALL PASSED

- [ ] **Step 5: Commit**

```bash
cd <repo-root>
git add src/stock_analyze_system/cli/watchlist.py src/stock_analyze_system/services/watchlist.py tests/unit/cli/test_watchlist_cli.py
git commit -m "feat: add watchlist CLI subcommand with Bug #17 fix (unified handler signature)"
```

---

## Task 9: target.py — 分析ターゲットサブコマンド

**Files:**
- Modify: `src/stock_analyze_system/cli/target.py`
- Create: `tests/unit/cli/test_target_cli.py`

- [ ] **Step 1: Write tests**

```python
# tests/unit/cli/test_target_cli.py
"""target CLI のテスト"""
import argparse
from unittest.mock import AsyncMock, MagicMock

import pytest

from stock_analyze_system.cli.target import handle
from stock_analyze_system.cli.helpers import ServiceContainer
from stock_analyze_system.exceptions import NotFoundError


def _make_services(**overrides):
    defaults = {
        "company_service": AsyncMock(),
        "financial_service": AsyncMock(),
        "valuation_service": AsyncMock(),
        "filing_service": AsyncMock(),
        "watchlist_service": AsyncMock(),
        "target_service": AsyncMock(),
        "job_service": AsyncMock(),
        "financial_sync": AsyncMock(),
        "filing_sync": AsyncMock(),
    }
    defaults.update(overrides)
    return ServiceContainer(**defaults)


class TestTargetList:
    async def test_list(self, capsys):
        svc = _make_services()
        t1 = MagicMock()
        t1.company_id = "US_AAPL"
        t1.source = "manual"
        t1.criteria = None
        svc.target_service.list_targets.return_value = [t1]

        args = argparse.Namespace(action="list", json=False)
        await handle(args, svc)
        out = capsys.readouterr().out
        assert "US_AAPL" in out

    async def test_list_empty(self, capsys):
        svc = _make_services()
        svc.target_service.list_targets.return_value = []

        args = argparse.Namespace(action="list", json=False)
        await handle(args, svc)
        out = capsys.readouterr().out
        assert "No" in out


class TestTargetAdd:
    async def test_add(self, capsys):
        svc = _make_services()
        svc.target_service.add_target.return_value = MagicMock()

        args = argparse.Namespace(
            action="add", json=False, company_id="US_AAPL",
        )
        await handle(args, svc)
        svc.target_service.add_target.assert_called_once_with("US_AAPL")
        out = capsys.readouterr().out
        assert "US_AAPL" in out


class TestTargetRemove:
    async def test_remove(self, capsys):
        svc = _make_services()

        args = argparse.Namespace(
            action="remove", json=False, company_id="US_AAPL",
        )
        await handle(args, svc)
        svc.target_service.remove_target.assert_called_once_with("US_AAPL")

    async def test_remove_not_found(self):
        svc = _make_services()
        svc.target_service.remove_target.side_effect = NotFoundError("not found")

        args = argparse.Namespace(
            action="remove", json=False, company_id="US_XXXX",
        )
        with pytest.raises(SystemExit):
            await handle(args, svc)
```

- [ ] **Step 2: Run tests — verify they fail**

Run: `cd <repo-root> && python3 -m pytest tests/unit/cli/test_target_cli.py -v --no-header 2>&1 | tail -20`
Expected: FAIL

- [ ] **Step 3: Implement target.py**

```python
# src/stock_analyze_system/cli/target.py
"""分析ターゲットサブコマンド"""
from __future__ import annotations

import argparse
import sys
from typing import TYPE_CHECKING

from stock_analyze_system.cli.formatters import format_json, format_table
from stock_analyze_system.exceptions import NotFoundError

if TYPE_CHECKING:
    from stock_analyze_system.cli.helpers import ServiceContainer


def register_parser(subparsers: argparse._SubParsersAction) -> None:
    parser = subparsers.add_parser("target", help="分析ターゲット管理")
    sub = parser.add_subparsers(dest="action")

    sub.add_parser("list", help="ターゲット一覧")

    add_p = sub.add_parser("add", help="ターゲット追加")
    add_p.add_argument("company_id", type=str)

    rm_p = sub.add_parser("remove", help="ターゲット削除")
    rm_p.add_argument("company_id", type=str)

    parser.set_defaults(handler=handle)


async def handle(args: argparse.Namespace, services: ServiceContainer) -> None:
    if not getattr(args, "action", None):
        sys.exit(1)

    handlers = {
        "list": _handle_list,
        "add": _handle_add,
        "remove": _handle_remove,
    }
    await handlers[args.action](args, services)


async def _handle_list(args: argparse.Namespace, services: ServiceContainer) -> None:
    targets = await services.target_service.list_targets()
    if not targets:
        print("No analysis targets.")
        return

    rows = [
        {"Company": t.company_id, "Source": t.source, "Criteria": t.criteria or ""}
        for t in targets
    ]
    if args.json:
        print(format_json(rows))
    else:
        print(format_table(rows))


async def _handle_add(args: argparse.Namespace, services: ServiceContainer) -> None:
    await services.target_service.add_target(args.company_id)
    print(f"Added '{args.company_id}' to analysis targets.")


async def _handle_remove(args: argparse.Namespace, services: ServiceContainer) -> None:
    try:
        await services.target_service.remove_target(args.company_id)
    except NotFoundError as e:
        print(str(e), file=sys.stderr)
        sys.exit(1)
    print(f"Removed '{args.company_id}' from analysis targets.")
```

- [ ] **Step 4: Run tests — verify they pass**

Run: `cd <repo-root> && python3 -m pytest tests/unit/cli/test_target_cli.py -v --no-header 2>&1 | tail -20`
Expected: ALL PASSED

- [ ] **Step 5: Commit**

```bash
cd <repo-root>
git add src/stock_analyze_system/cli/target.py tests/unit/cli/test_target_cli.py
git commit -m "feat: add target CLI subcommand (list/add/remove)"
```

---

## Task 10: serve.py (Bug #15, 新1) + screen.py stub + rag.py stub

**Files:**
- Modify: `src/stock_analyze_system/cli/serve.py`
- Modify: `src/stock_analyze_system/cli/screen.py`
- Modify: `src/stock_analyze_system/cli/rag.py`
- Create: `tests/unit/cli/test_serve_cli.py`

Bug #15 修正: `args.port is not None` チェック
新1 修正: ポート範囲バリデーション (1–65535)

- [ ] **Step 1: Write serve tests**

```python
# tests/unit/cli/test_serve_cli.py
"""serve CLI のテスト (Bug #15, 新1 修正含む)"""
import argparse

import pytest

from stock_analyze_system.cli.serve import register_parser


class TestBug15PortZero:
    """Bug #15: ポート0問題のテスト"""

    def test_port_not_specified_uses_default(self):
        parser = argparse.ArgumentParser()
        sub = parser.add_subparsers(dest="command")
        register_parser(sub)
        args = parser.parse_args(["serve"])
        # port はデフォルトで None（configから取得するため）
        assert args.port is None

    def test_port_explicit_value(self):
        parser = argparse.ArgumentParser()
        sub = parser.add_subparsers(dest="command")
        register_parser(sub)
        args = parser.parse_args(["serve", "--port", "3000"])
        assert args.port == 3000


class TestNew1PortValidation:
    """新1: ポート範囲バリデーション"""

    def test_port_negative_rejected(self):
        parser = argparse.ArgumentParser()
        sub = parser.add_subparsers(dest="command")
        register_parser(sub)
        with pytest.raises(SystemExit):
            parser.parse_args(["serve", "--port", "-1"])

    def test_port_zero_rejected(self):
        parser = argparse.ArgumentParser()
        sub = parser.add_subparsers(dest="command")
        register_parser(sub)
        with pytest.raises(SystemExit):
            parser.parse_args(["serve", "--port", "0"])

    def test_port_too_large_rejected(self):
        parser = argparse.ArgumentParser()
        sub = parser.add_subparsers(dest="command")
        register_parser(sub)
        with pytest.raises(SystemExit):
            parser.parse_args(["serve", "--port", "70000"])

    def test_port_valid_range(self):
        parser = argparse.ArgumentParser()
        sub = parser.add_subparsers(dest="command")
        register_parser(sub)
        args = parser.parse_args(["serve", "--port", "8080"])
        assert args.port == 8080
```

- [ ] **Step 2: Run tests — verify they fail**

Run: `cd <repo-root> && python3 -m pytest tests/unit/cli/test_serve_cli.py -v --no-header 2>&1 | tail -20`
Expected: FAIL

- [ ] **Step 3: Implement serve.py**

```python
# src/stock_analyze_system/cli/serve.py
"""Webサーバー起動サブコマンド (Bug #15修正: port is not None, 新1修正: 範囲検証)"""
from __future__ import annotations

import argparse
from typing import TYPE_CHECKING

if TYPE_CHECKING:
    from stock_analyze_system.config import AppConfig


def _valid_port(value: str) -> int:
    """新1修正: ポート範囲バリデーション (1-65535)"""
    port = int(value)
    if port < 1 or port > 65535:
        raise argparse.ArgumentTypeError(
            f"Port must be between 1 and 65535, got {port}",
        )
    return port


def register_parser(subparsers: argparse._SubParsersAction) -> None:
    parser = subparsers.add_parser("serve", help="Webサーバー起動")
    parser.add_argument(
        "--host", type=str, default=None,
        help="バインドホスト (default: configから取得)",
    )
    parser.add_argument(
        "--port", type=_valid_port, default=None,
        help="バインドポート (default: configから取得)",
    )
    parser.set_defaults(handler=handle)


async def handle(args: argparse.Namespace, config: AppConfig) -> None:
    import uvicorn

    # Bug #15修正: 0 はfalsyなので or ではなく is not None で判定
    host = args.host if args.host is not None else config.web.host
    port = args.port if args.port is not None else config.web.port

    print(f"Starting server on {host}:{port}...")
    uvicorn.run(
        "stock_analyze_system.web:create_app",
        host=host,
        port=port,
        factory=True,
    )
```

- [ ] **Step 4: Implement screen.py stub**

```python
# src/stock_analyze_system/cli/screen.py
"""スクリーニングサブコマンド (Phase 5依存 — スタブ)"""
from __future__ import annotations

import argparse
import sys
from typing import TYPE_CHECKING

if TYPE_CHECKING:
    from stock_analyze_system.cli.helpers import ServiceContainer


def register_parser(subparsers: argparse._SubParsersAction) -> None:
    parser = subparsers.add_parser("screen", help="スクリーニング")
    sub = parser.add_subparsers(dest="action")

    sub.add_parser("run", help="スクリーニング実行")
    sub.add_parser("add-targets", help="結果をターゲットに追加")

    parser.set_defaults(handler=handle)


async def handle(args: argparse.Namespace, services: ServiceContainer) -> None:
    print("Screen command requires Phase 5 (ScreeningService). Not yet implemented.",
          file=sys.stderr)
    sys.exit(1)
```

- [ ] **Step 5: Implement rag.py stub**

```python
# src/stock_analyze_system/cli/rag.py
"""RAG分析サブコマンド (Phase 6依存 — スタブ)"""
from __future__ import annotations

import argparse
import sys
from typing import TYPE_CHECKING

if TYPE_CHECKING:
    from stock_analyze_system.cli.helpers import ServiceContainer


def register_parser(subparsers: argparse._SubParsersAction) -> None:
    parser = subparsers.add_parser("rag", help="RAG分析")
    sub = parser.add_subparsers(dest="action")

    sub.add_parser("analyze", help="定型分析実行")
    sub.add_parser("ask", help="自由質問")
    sub.add_parser("index", help="インデックス構築")
    sub.add_parser("status", help="インデックス状態")
    sub.add_parser("health", help="LLMヘルスチェック")
    sub.add_parser("show", help="分析結果表示")

    parser.set_defaults(handler=handle)


async def handle(args: argparse.Namespace, services: ServiceContainer) -> None:
    print("RAG command requires Phase 6 (RagService). Not yet implemented.",
          file=sys.stderr)
    sys.exit(1)
```

- [ ] **Step 6: Run serve tests — verify they pass**

Run: `cd <repo-root> && python3 -m pytest tests/unit/cli/test_serve_cli.py -v --no-header 2>&1 | tail -20`
Expected: ALL PASSED

- [ ] **Step 7: Commit**

```bash
cd <repo-root>
git add src/stock_analyze_system/cli/serve.py src/stock_analyze_system/cli/screen.py src/stock_analyze_system/cli/rag.py tests/unit/cli/test_serve_cli.py
git commit -m "feat: add serve (Bug #15, port validation), screen stub, rag stub"
```

---

## Task 11: 全テスト実行 + ruff lint + 最終コミット

**Files:**
- All Phase 4 files

- [ ] **Step 1: 全テスト実行**

Run: `cd <repo-root> && python3 -m pytest tests/ -v --tb=short 2>&1 | tail -40`
Expected: ALL PASSED（Phase 1 + Phase 2 + Phase 3 + Phase 4 の全テスト）

- [ ] **Step 2: ruff lint 実行**

Run: `cd <repo-root> && python3 -m ruff check src/stock_analyze_system/cli/ tests/unit/cli/ 2>&1`
Expected: All checks passed（または修正可能な警告のみ）

- [ ] **Step 3: lint問題があれば修正**

Run: `cd <repo-root> && python3 -m ruff check --fix src/stock_analyze_system/cli/ tests/unit/cli/ 2>&1`

- [ ] **Step 4: 再テスト**

Run: `cd <repo-root> && python3 -m pytest tests/ -v --tb=short 2>&1 | tail -20`
Expected: ALL PASSED

- [ ] **Step 5: コミット (lint修正がある場合のみ)**

```bash
cd <repo-root>
git add src/stock_analyze_system/cli/ tests/unit/cli/
git commit -m "fix: resolve ruff lint issues in Phase 4 CLI"
```
